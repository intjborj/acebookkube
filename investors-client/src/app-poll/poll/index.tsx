import React from 'react'
import PollContent from './components/pollContent'

type Props = {}

const PollIndex = (props: Props) => {
    return (
        <div>
            <PollContent />
        </div>
    )
}

export default PollIndex